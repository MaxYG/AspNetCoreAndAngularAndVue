快捷键
search everywhere: Double shift
Go to file: ctrl shift n
recent files: ctrl e
navigation bar: alt home

如何共享登录用户到各个组件上去


angularjs2

`
//todo:

几个概念
zone.js
rxjs
promise
typescript
es6
调试
多语言
错误信息提示



3.路由中带参跳转不能展现详细页面 这个是数组出了问题
1.如何让组件自动编译到其它文件夹里
5.模拟webapi 怎么耍，什么原理
6.数组引用怎么整 差看demo dashboard例子
7.<input #heroName /> 这个语法...
8.内存模拟 id会自增



angularjs2 写法
样式最好不要用- eg: color-red 用 colorRed  ngstyle 和ngclass都可以用

遇到问题
1.在国际化时遇到问题 升级部分包
TypeError: Cannot read property 'create' of undefined
    at Function.Extractor.create (F:\study\Angularjs2


    注意：
    1.永远不要在特性路由模块中调用RouterModule.forRoot！
    2.管道：一个向服务器发起HTTP请求的管道。 这通常是一个可怕的主意。
    不管我们怎么做，估计它都会是一个可怕的主意。
     但即便如此，为了讲解这个技术点，我们还是写一个吧。 时刻记住，
    非纯管道可能每隔几微秒就会被调用一次。 如果我们不小心点，
    这个管道就会发起一大堆请求“攻击”服务器。
    3.管道：但是一个纯管道必须总是用纯函数实现。忽略这个警告将导致失败并带来一大堆这样的控制台错误：
    表达式在被检查后被变更

    安全：注入DomSanitizer服务，然后调用下面的方法之一，你就可以把一个值标记为可信任的。

       bypassSecurityTrustHtml
       bypassSecurityTrustScript
       bypassSecurityTrustStyle
       bypassSecurityTrustUrl
       bypassSecurityTrustResourceUrl
